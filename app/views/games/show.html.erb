<div class="game_container">

  <div class="total_scores">
    <p>Total Scores</p>
    <% @game.players.order(:id).each do |player| %>
      <p><%= player.player_name %> - <%= player.total_score %></p>
    <% end %>
  </div>

  <%= render 'game' %>

</div>
